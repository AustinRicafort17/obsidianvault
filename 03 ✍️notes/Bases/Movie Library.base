views:
  - type: cards
    name: Library
    filters:
      and:
        - file.hasLink("02 LIBRARIES/Movies.md")
        - "!title.isEmpty()"
    order:
      - file.name
      - rating
    image: note.cover
    imageAspectRatio: 1.5
    cardSize: 170
