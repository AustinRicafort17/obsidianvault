filters:
  and:
    - "!title.isEmpty()"
views:
  - type: cards
    name: Library
    filters:
      and:
        - file.hasLink("Categories/Books.md")
        - file.hasTag("books")
    order:
      - file.name
      - rating
    imageAspectRatio: 1.25
    image: note.cover
    cardSize: 200
