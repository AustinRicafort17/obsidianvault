filters:
  and:
    - "!title.isEmpty()"
views:
  - type: cards
    name: Library
    filters:
      and:
        - file.hasLink("02 LIBRARIES/Books.md")
        - file.hasTag("books")
    order:
      - file.name
      - rating
    imageAspectRatio: 1.25
    image: note.cover
    cardSize: 200
